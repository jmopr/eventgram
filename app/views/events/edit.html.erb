<%= render 'form' %>
  <div>
    <%= link_to (image_tag @event.photo.url(:medium), class:'img-responsive') %>
  </div>
  <div class="text-center edit-links">
    <%= link_to "Delete Post", event_path(@event), method: :delete, data: { confirm: "Are you sure you want to delete this post?" } %> |
    <%= link_to "Cancel", events_path %>
  </div>
